apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssp-lls.fullname" . }}
  labels:
    {{- include "ssp-lls.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssp-lls.selectorLabels" . | nindent 4 }}
