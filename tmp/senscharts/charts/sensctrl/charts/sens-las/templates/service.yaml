apiVersion: v1
kind: Service
metadata:
  name: {{ include "sens-las.fullname" . }}
  labels:
    {{- include "sens-las.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sens-las.selectorLabels" . | nindent 4 }}
