apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sens-las.fullname" . }}-test-connection"
  labels:
    {{- include "sens-las.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sens-las.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
