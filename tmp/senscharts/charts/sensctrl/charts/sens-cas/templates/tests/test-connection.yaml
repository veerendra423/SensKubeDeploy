apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sens-cas.fullname" . }}-test-connection"
  labels:
    {{- include "sens-cas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sens-cas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
