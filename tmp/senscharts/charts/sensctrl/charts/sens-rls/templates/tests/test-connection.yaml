apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sens-rls.fullname" . }}-test-connection"
  labels:
    {{- include "sens-rls.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sens-rls.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
