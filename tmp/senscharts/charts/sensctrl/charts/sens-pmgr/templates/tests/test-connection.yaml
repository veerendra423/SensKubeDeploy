apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sens-pmgr.fullname" . }}-test-connection"
  labels:
    {{- include "sens-pmgr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sens-pmgr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
