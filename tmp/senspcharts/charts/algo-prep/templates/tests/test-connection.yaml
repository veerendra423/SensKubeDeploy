apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "algo-prep.fullname" . }}-test-connection"
  labels:
    {{- include "algo-prep.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "algo-prep.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
